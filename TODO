== figure out the singleton AsyncHttpClient asyncHttpClient__ in CuracaoExampleDispatcherServlet
    and how should it be "shutdown" on application stop

== component injection when instantiating a new controller in InvokableClassWithInstance
    -- use @Injectable
    -- see Set<Constructor> injectables = reflections.getConstructorsAnnotatedWith(javax.inject.Inject.class);

== check default character encoding MemoryBufferingRequestBodyMapper.getRequestEncoding() ... UTF 8 or ISO 8859 by default?

